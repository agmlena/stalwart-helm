# Patterns to ignore when building packages.
.git
.gitignore
.github/
.vscode/
.claude/
.DS_Store
*.swp
*.bak
*.tmp
*~
# helm-docs template (build artifact, not needed in packaged chart)
README.md.gotmpl
